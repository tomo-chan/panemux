\* A .cfg with no MaxEntries constant, used to assert the exporter refuses to
\* guess a bound rather than deriving one from whatever it happened to see.
SPECIFICATION Spec

CONSTANTS
    MaxRecords = 2

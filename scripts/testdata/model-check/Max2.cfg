\* A deliberately small bound (MaxEntries = 2) used only to generate the dot
\* fixtures in this directory. See README.md.
SPECIFICATION Spec

CONSTANTS
    MaxEntries = 2
    MaxRecords = 2

INVARIANTS
    TypeOK
    Conservation
    NoOverMatch
